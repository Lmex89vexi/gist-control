[project]
name = "gistman"
version = "0.1.0"
description = "Interactive TUI for managing GitHub Gists"
requires-python = ">=3.10"
dependencies = [
    "textual>=0.50.0",
    "pyperclip>=1.8",
]

[project.scripts]
gistman = "gistman:app.run"
